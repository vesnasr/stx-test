
import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

import order from './order';

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: {
    order
  }
});
